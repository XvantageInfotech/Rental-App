package com.xvantage.rental.utils.libs

import android.app.Activity
/*import com.airysnackbar.AirySnackbar
import com.airysnackbar.AirySnackbarSource
import com.airysnackbar.AirySnackbarType
import com.airysnackbar.TextAttribute
import com.airysnackbar.IconAttribute
import com.airysnackbar.SizeAttribute
import com.airysnackbar.RadiusAttribute
import com.airysnackbar.GravityAttribute
import com.airysnackbar.AnimationAttribute
import com.airysnackbar.SizeUnit*/

object SnackbarUtil {
/*
    // Show Success Snackbar
    fun showSuccessSnackbar(activity: Activity, message: String) {
        AirySnackbar.make(
            source = AirySnackbarSource.ActivitySource(activity = activity),
            type = AirySnackbarType.Success,
            attributes = listOf(
                TextAttribute.Text(text = message),
                TextAttribute.TextColor(textColor = R.color.white), // Customize text color
                IconAttribute.Icon(iconRes = R.drawable.ic_check),
                IconAttribute.IconColor(iconTint = R.color.green),
                SizeAttribute.Margin(left = 24, right = 24, unit = SizeUnit.DP),
                SizeAttribute.Padding(top = 12, bottom = 12, unit = SizeUnit.DP),
                RadiusAttribute.Radius(radius = 8f),
                GravityAttribute.Top,
                AnimationAttribute.FadeInOut
            )
        ).show()
    }

    // Show Alert/Error Snackbar
    fun showErrorSnackbar(activity: Activity, message: String) {
        AirySnackbar.make(
            source = AirySnackbarSource.ActivitySource(activity = activity),
            type = AirySnackbarType.Error,
            attributes = listOf(
                TextAttribute.Text(text = message),
                TextAttribute.TextColor(textColor = R.color.white), // Customize text color
                IconAttribute.Icon(iconRes = R.drawable.ic_error),
                IconAttribute.IconColor(iconTint = R.color.red),
                SizeAttribute.Margin(left = 24, right = 24, unit = SizeUnit.DP),
                SizeAttribute.Padding(top = 12, bottom = 12, unit = SizeUnit.DP),
                RadiusAttribute.Radius(radius = 8f),
                GravityAttribute.Top,
                AnimationAttribute.FadeInOut
            )
        ).show()
    }

    // Show Custom Snackbar with background color
    fun showCustomSnackbar(activity: Activity, message: String, bgColor: Int) {
        AirySnackbar.make(
            source = AirySnackbarSource.ActivitySource(activity = activity),
            type = AirySnackbarType.Custom(bgColor = bgColor),
            attributes = listOf(
                TextAttribute.Text(text = message),
                TextAttribute.TextColor(textColor = R.color.white),
                IconAttribute.Icon(iconRes = R.drawable.ic_custom),
                IconAttribute.IconColor(iconTint = R.color.blue),
                SizeAttribute.Margin(left = 24, right = 24, unit = SizeUnit.DP),
                SizeAttribute.Padding(top = 12, bottom = 12, unit = SizeUnit.DP),
                RadiusAttribute.Radius(radius = 8f),
                GravityAttribute.Top,
                AnimationAttribute.FadeInOut
            )
        ).show()
    }

    // Show Info Snackbar (if needed)
    fun showInfoSnackbar(activity: Activity, message: String) {
        AirySnackbar.make(
            source = AirySnackbarSource.ActivitySource(activity = activity),
            type = AirySnackbarType.Info,
            attributes = listOf(
                TextAttribute.Text(text = message),
                TextAttribute.TextColor(textColor = R.color.white),
                IconAttribute.Icon(iconRes = R.drawable.ic_info),
                IconAttribute.IconColor(iconTint = R.color.blue),
                SizeAttribute.Margin(left = 24, right = 24, unit = SizeUnit.DP),
                SizeAttribute.Padding(top = 12, bottom = 12, unit = SizeUnit.DP),
                RadiusAttribute.Radius(radius = 8f),
                GravityAttribute.Top,
                AnimationAttribute.FadeInOut
            )
        ).show()
    }

    // Show Warning Snackbar
    fun showWarningSnackbar(activity: Activity, message: String) {
        AirySnackbar.make(
            source = AirySnackbarSource.ActivitySource(activity = activity),
            type = AirySnackbarType.Warning,
            attributes = listOf(
                TextAttribute.Text(text = message),
                TextAttribute.TextColor(textColor = R.color.black),
                IconAttribute.Icon(iconRes = R.drawable.ic_warning),
                IconAttribute.IconColor(iconTint = R.color.yellow),
                SizeAttribute.Margin(left = 24, right = 24, unit = SizeUnit.DP),
                SizeAttribute.Padding(top = 12, bottom = 12, unit = SizeUnit.DP),
                RadiusAttribute.Radius(radius = 8f),
                GravityAttribute.Top,
                AnimationAttribute.FadeInOut
            )
        ).show()
    }*/
}
